# Skibidibi Git Store assets

This orphan branch contains immutable, versioned installer files referenced by external
distribution forms. Never replace an existing file after its URL has been submitted. Publish a
new application version and path instead.
